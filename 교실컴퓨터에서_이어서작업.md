# 교실 컴퓨터에서 이어서 작업하기 — 요청 078
## 목적

이 문서는 개발 코드를 교실 컴퓨터에서 이어서 받기 위한 안내입니다.
실제 학생 계정·교실·아이템 데이터를 옮기는 안내가 아닙니다.

## 시작 전
- Git과 Node.js 24가 설치된 컴퓨터에서 진행합니다.
- 일반 사용자는 도우미에게 아래 명령어 실행을 맡기면 됩니다.
- GitHub 최신 `main`은 https://github.com/rkdtjdwns1312-wq/space_metaverse.git 입니다.
- 이미 `space_metaverse` 폴더가 있으면 먼저 그 폴더의 로컬 수정을 보존합니다.
- 폴더가 있으면 다음을 실행합니다.
```text
cd space_metaverse
git pull --ff-only origin main
```

- 폴더가 없으면 다음을 실행합니다.
```text
git clone https://github.com/rkdtjdwns1312-wq/space_metaverse.git
cd space_metaverse
```

- Git가 명령어로 인식되지 않으면 이미 설치된 Git을 찾아 전체 경로로 실행합니다.
- `git reset --hard`는 사용하지 않습니다. 로컬 수정과 교실 자료를 지우면 안 됩니다.

## 이어서 확인할 파일
다음 문장을 도우미에게 그대로 붙여넣어 주세요.
```text
이 저장소의 main을 최신으로 받아 메인.md의 최신 현재 상태에서 안내한 최신 기록을 찾아 읽고 현재 구현 상태와 미완료 사항을 확인해줘. 기존 로컬 변경과 교실 데이터는 보존하고 실행 환경을 준비해줘.
```

## 최신 구현 / 다음 확인

- 최신 `main`에는 Lv2 별자리 16종의 게임용 2D 이미지와 별자리별 능력이 반영되어 있습니다. 능력은 한국 시간 기준 주 1회 사용할 수 있고 다음 월요일에 다시 열립니다.
- Lv1 아이템 카드 8종과 달토끼의 하루 1회 카드 뽑기, 소행성의 Lv2 진화 후 별자리 아바타 전환을 구현했습니다. 상점·뽑기·진화 화면에서 직접 확인합니다.
- Lv3~4 진화 그림과 아이템 카드는 아직 원본 자료나 별도 설계가 필요합니다. 몬스터 사냥 연결도 준비 중이며, 임의로 시작하지 않습니다.
- 실행 환경과 공개 접속 정보는 하드코딩된 값을 사용하지 말고 `메인.md`의 최신 현재 상태와 최신 기록에서 확인합니다. 서버를 다시 시작한 뒤에는 교사 메뉴에서 현재 학생 링크를 다시 복사합니다.

## 개발 환경 확인
저장소 폴더에서 다음을 순서대로 실행합니다.
```text
corepack pnpm install --frozen-lockfile
node --test tests/*.test.js
node scripts/verify-browser.mjs
.\교실-시작.cmd
```

마지막 명령은 개발 서버를 시작하는 명령입니다.

## 데이터와 서버 안내
- 코드·그림·문서·테스트만 GitHub로 동기화됩니다.
- 실제 학생 계정·교실·아이템 데이터(`data/classes`)와 `.env`(교사 키), `.local`은 Git에 없습니다.
- 이 자료가 필요하면 별도 안전한 이전 절차를 따릅니다. 이번 요청에서는 이전하지 않습니다.
- 개발은 새 테스트 교실로 진행할 수 있습니다.
- 기존 서버와 공개 주소는 이 PC의 서버입니다.
- 학교 컴퓨터에서 `pull`해도 기존 서버나 공개 주소가 교실 컴퓨터로 이전되지는 않습니다.
- 기존 교실 컴퓨터에 실제로 설치해 검증한 것은 아직 아닙니다.
- 다음 기능은 사용자의 새 지시 없이 임의로 시작하지 않습니다.

## 화면이 예전 모습일 때
- GitHub에 push해도 다른 컴퓨터에서 실행 중인 서버가 자동으로 업데이트되지는 않습니다.
- 그 컴퓨터의 로컬 변경을 보존한 뒤 `git pull --ff-only origin main`을 실행합니다.
- 실행 중인 서버를 정상 종료하고 `교실-시작.cmd` 또는 `외부접속-교실-시작.cmd`를 다시 실행합니다.
- 브라우저를 새로고침합니다.
- 외부 주소는 서버를 다시 시작할 때 바뀌므로, 교사 메뉴에서 학생 링크를 다시 복사합니다.
- `data/classes`와 `.env`는 Git으로 동기화되지 않으며 덮어쓰면 안 됩니다.

## 결과 보고
명령어별 성공·실패, 오류 내용, 실제 설치 검증 여부를 도우미에게 보고받습니다.
이번 안내문 작성 자체에서는 교실 데이터 이전과 기능 개발을 하지 않습니다.
