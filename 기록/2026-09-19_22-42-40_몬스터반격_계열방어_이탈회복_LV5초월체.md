# 몬스터 반격·계열 방어·이탈 회복·LV5 초월체 (요청140~149)

- 작업 도구: Codex
- 구현·최종 검수 모델: gpt-6-astra (Astra), 직접 검수
- 하위 작업: gpt-5.6-luna / low — Gibbs(전투 설정 검사), Feynman(반격 표시), Avicenna(LV5 검사 갱신), Popper(README 초안). 결과는 Astra가 통합·수정·검증.
- 기록 시각: 2026-09-19 22:42:40 KST

## 1. 요청 요약

140 초월체 HP/MP40·공격4. 141 몬스터 선공 없이 공격받은 뒤 추적/반격2·3·5. 142 다음 맵마다 이동과 행동 빈도30% 증가. 143 방어력만큼 피해 감소·최소1. 144 최종 단계를 LV5 초월체로 정정. 145 생산/제작 방어−1·공격/특수0·수호+1·하한0. 146 공격자 중 수호>특수>그 외, 같은 계열 우선순위 내 최근 공격자. 147 공격 대상 소멸 시 전회복. 148 공격 대상 맵 이탈 누적3회 전회복. 149 현재 전투 수치 표 제공.

## 2. 계획

서버 권위 전투 상태와 대상 선정을 분리하고 실제 소켓/브라우저로 검증한다. 저장 레벨6의 하위 호환을 보존한다. 몬스터의 회복은 기존 처치/10초 재등장과 분리한다. 실제 학급 파일을 시험에 사용하지 않는다.

## 3. 수행 내용과 변경 파일

- `shared/combat.js`, `vitals.js`: 공격 LV2/3/4/5=1/2/3/4, HP/MP LV1~5=1/10/20/30/40, 기본 방어0/0/1/2/3와 계열 보정. LV1 항상0. 최소 피해1.
- `shared/config.js`, `constellations.js`, `avatar-label.js`, `server/progression.js`, `persistent-rooms.js`, 진화/성장 UI: LV4 XP30→LV5 초월체, XP0·최고 단계. 구 LV6는 원본 객체를 훼손하지 않고 LV5로 읽는다. 계보·자산·16종 별빛 원화 유지.
- `server/vitals.js`: 현재 HP/MP를 실행 중 서버 상태로 유지. 내 정보 조회·상태 전송·재접속으로 회복하지 않는다. 서버 재시작 시 최대치.
- `server/monsters.js`: 실제 공격자 목록·공격 순번·계열 우선순위. 죽음/접속해제/맵이탈/검은별 후보 제거. 미공격자는 계열과 무관하게 제외.
- 몬스터 HP20/40/100, 공격2/3/5. 이동속도36/46.8/60.84 px/s, 방향 변경 및 공격 간격1000/(1,1.3,1.69)ms. 서버가 근접과 방어력을 계산한다.
- 공격자 모두 무효이면 살아 있는 몬스터 즉시 전회복. 남은 공격자 있으면 유지. 마지막 공격자를 쓰러뜨린 경우도 즉시 재선정/회복.
- 공격자 맵 이탈을 몬스터별 누적해3회에 전회복·횟수0. 남은 공격자는 계속 추적하고 공격 간격 유지. 이탈자는 후보에서 제거되어 틱마다 중복 집계되지 않는다. 다시 들어와 **다시 공격**한 뒤 나가면 다음 이탈로 집계한다. 비공격자·같은 맵의 단순 접속해제는 이탈 횟수에 넣지 않는다. 모두 사라져 회복할 때/처치/재등장 때도 횟수0.
- 새 공격 직전에 기존 후보를 정리하여, 직전 공격자의 이탈 회복이 새 공격 피해를 지우지 않도록 했다. 처치된 몬스터는 회복 규칙으로 즉시 부활하지 않는다.
- `server/battle-recovery.js`, `world.js`, `app.js`: HP0 이동/공격/맵 이동·진화 중단, 3초 후 광장 HP/MP 회복. 아이템/재화 차감 없음. 검은별이면 블랙홀로 복귀하여 제한 우회 방지.
- `client/world.js`: 아바타 모션 변경 없이 분홍/주황 타격과 피해 숫자. `app.js`, `combat-controls.js`, `index.html`: 실시간 HP와 내 정보 방어력, HP0 안내.
- README에 계열별 전투 표·현행 운영 규칙·새 검증 명령 기록. 이전 작업의 일반 LV5/내부 LV6 구조는 본 기록으로 대체한다.

## 4. 검증 결과

- `node --test --test-concurrency=1 tests/*.test.js`: **256/256 PASS**.
- `node scripts/verify-browser.mjs`: **91건 PASS**, 오류0.
- `node scripts/verify-retaliation.mjs`: **9건 PASS** — 선공없음, 실제 Q 반격/HP 감소, 추적/거리, 실제 소켓 수호→특수→나머지, 맵 이탈3회 회복, 마지막 공격자 쓰러짐 전회복, HP0 이동/공격 금지, 3초 복귀, 계열별 LV5 방어.
- 조작9·지도18·몬스터/행성10·별빛 진화6 PASS. 16종 모두 실제 UI에서 LV4→LV5 초월체 진화/계보/XP0 확인.
- 단위검사에 같은 이탈 중복 방지, 미공격자 이탈 제외, 재입장만으로 재집계 금지, 마지막 이탈 후 새 피해 보존, 죽은 몬스터10초 대기, 방 격리, 최소 피해, 전 단계·16종 방어 보정 포함.
- 모바일390px HP0 화면 직접 검수: 체력0/10·마나10/10·몬스터20/20·회복 안내·원형 버튼·하단 메뉴 확인.
- 공개 서버 재시작 PID21808, 새 주소 `https://speeds-displayed-bool-kruger.trycloudflare.com`, 로컬3000. 공개 정적33파일 SHA256 일치·WebSocket 연결·지속 저장 health 정상, 실제 학급4파일 SHA256 불변 확인 완료.

## 5. 최종 점검(검수)

Astra가 공격자 선정·피해·회복과 저장 호환을 직접 검수했다. 하위 결과 중 진화 뒤 form 예상값, 잘못된 README LV5 다음 단계/몬스터 속도의 아바타 적용 문구를 수정했다. 단위 테스트의 기존 간소화 공격자 fixture를 실제 학생 상태로 보강했다. 비밀키/개인정보는 공개 검증에 사용하지 않았다.

## 6. 미완료 사항과 다음 단계

- W는 방향 표시와 준비 안내만 제공한다. 실제 전투 스킬·MP 소비·사냥 경험치/재화 보상은 미연결.
- HP0 처리 선택 질문에는 답이 도착하지 않아, 미리 안내한 기본안(3초 후 광장·손실 없음)을 적용했다. 이후 수업 운영 기준에 맞게 변경 가능.
- 현재 HP·몬스터 상태·이탈 횟수는 실행 중 상태이며 서버 재시작 시 초기화한다. 학생 재화·아이템·계보는 기존 영구 저장 유지.
- 공개 무료 주소는 실행기를 다시 시작하면 바뀐다. 학생 이용 시간은 한국07~21시, 서버는 계속 켜 두어야 한다.

## 7. Git 동기화 결과

- 작업 시작 시 `main`의 `38d8c603fe476f627f0a9ca99b9c26c3133a941c` 기준 pull --ff-only 완료.
- 기능/기록 커밋 `1adcf1cfbf7229816990e5c33f603acf63b02d95` push 완료. 로컬 HEAD=origin/main=실제 원격 refs/heads/main 일치 확인.
- 이 동기화 결과를 문서 보완 커밋으로 push하고 작업 트리 clean을 다시 확인한다.
