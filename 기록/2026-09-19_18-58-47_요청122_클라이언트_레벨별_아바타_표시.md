# 요청122 클라이언트 레벨별 아바타 표시

- 작업 도구: Codex
- 사용 모델: 하위 에이전트 gpt-5.6-luna (low)
- 검수 모델: gpt-6-astra(Astra) 본체 통합 검수(최종 요청122 기록 참조)
- 작업 시각: 2026-09-19 18:58:47 KST

## 1 요청 요약

Lv3/Lv4 PPT 아바타 API 확장에 맞춰 `client/world.js`와 `client/app.js`의 조회·표시만 연결한다. 기존 이동 방식과 능력 사용 로직·필터는 유지한다.

## 2 계획

두 클라이언트 파일의 모든 `constellationOf` 호출을 찾고, 호출 대상 아바타의 `level`을 전달한다. 월드 아바타, 자기 초상화, 능력 카드와 제목·alt에 Lv를 표시한 뒤 지정된 문법 검사만 실행한다.

## 3 수행 내용과 변경 파일

- `client/world.js`: 다른 플레이어와 자기 아바타 렌더링에서 `constellationOf(id, avatar.level)`을 사용했다.
- `client/app.js`: 능력 조회, 자기 설명, 별자리 유형, 능력 카드 조회에 레벨을 전달했다. 능력 제목·자기 설명·능력 카드 이름과 이미지 alt에 Lv를 추가했다.
- `메인.md`: 요청122 상태와 결과를 갱신했다.
- `기록/2026-09-19_18-58-47_요청122_클라이언트_레벨별_아바타_표시.md`: 작업 기록을 작성했다.

## 4 검증 결과

- `constellationOf(` 호출 검색 결과: `client/world.js` 2곳, `client/app.js` 4곳 모두 레벨 인자 전달 확인.
- `node --check client/world.js`: 통과.
- `node --check client/app.js`: 통과.
- 본체가 위임한 조회·표시 범위에서는 문법 검사만 실행했다. 최종 서버/브라우저 검사는 본체 통합 기록을 따른다.
- 작업 시작 시 `git pull --ff-only`는 `.git/FETCH_HEAD` 권한 오류로 실패했다.

## 5 최종 점검(검수)

두 파일의 조회·표시 변경을 직접 검토했다. 기존 이동·능력 사용·타겟 레벨·pending shop-copy 필터 로직은 변경하지 않았다. 본체가 담당하는 `shared/constellations.js` API 구현과 `server/evolution.js`는 건드리지 않았다.

## 6 미완료 사항과 다음 단계

이 문서는 요청122의 중간 하위 작업 기록이다. 본체가 이후 API·능력·32종 이미지 연결을 완료했으며 미완료 여부와 검사 결과는 최종 요청122 통합 기록을 따른다.

## 7 Git 동기화 결과

중간 커밋 `9b6a30b`에 표시 변경과 당시 본체의 공유 API 변경이 포함되어 push되었다. 최종 이미지와 능력 구현은 후속 통합 커밋으로 반영한다. 최종 Git 상태는 요청122 통합 기록을 따른다.
