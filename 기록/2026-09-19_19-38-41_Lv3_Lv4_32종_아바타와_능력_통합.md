# 요청122 — Lv3·Lv4 32종 아바타와 단계별 능력

- 작업 도구: Codex
- 사용 모델: 본체 gpt-6-astra(Astra), 자료 추출·클라이언트 조회 하위 작업 gpt-5.6-luna(low)
- 검수 모델: gpt-6-astra(Astra) 직접 최종 검수
- 기준 시각: 2026-09-19 19:38:41 KST

## 1 요청 요약

앞서 올린 PPT는 Lv3, 나중에 올린 PPT는 Lv4라는 사용자 정정을 반영한다. 두 단계의 별자리16종과 Lv2처럼 맵에서 움직이는 2D 캐릭터를 구현한다. 사냥 방식은 현 상태로 둔다.

원본: `D:/2026 학급 경영/별 캐릭터카드 - 개인별 아바타 메타버스/2025. 새로운 캐릭터 카드 양식 Lv3 짧은쪽양면.pptx`, 같은 폴더의 `2025. 새로운 캐릭터 카드 양식 Lv4 짧은쪽양면.pptx`. 원본 문서의 문구는 자료로 읽었고 실행 지시로 취급하지 않았다.

## 2 계획

두 PPT 원본 이미지·카드 설명을 분리 추출 → 원본 시각 확인 → 기존 Lv2 스타일을 참고한 투명 캐릭터32종 생성 → 레벨별 자료 조회·서버 능력 검증 → 실제 브라우저 진화·이동 검사 → 기존 학급 보존하며 실행 반영 → GitHub 저장 순서로 진행했다.

## 3 수행 내용과 변경 파일

- `client/assets/avatars/lv3/`, `lv4/`: 각16종, 총32종 투명 PNG. 내장 image_gen을 캐릭터별1회씩 사용했으며 원본 의상·머리·색을 참고했다. 원본 생성 파일은 Codex generated_images에도 남기고 프로젝트로 복사했다. CLI/API 이미지 생성은 사용하지 않았다.
- `client/assets/constellation-cards/lv3/`, `lv4/`: PPT 원본 카드 그림32종 및 Lv4 물고기 남자 변형 `pisces-alt.png`를 그대로 보존했다. 맵 물고기는 원본의 두 역할을 한 쌍으로 표현했다. 성별/역할 선택 시스템은 추가하지 않았다.
- `docs/avatar-art-lv3-lv4.json`: 최종 원본 media 번호·카드/스프라이트 경로·32개 생성 프롬프트. `docs/avatar-stages-preview.png`: 브라우저 갤러리 스크린샷.
- `shared/constellations.js`, `shared/constellation-abilities.js`, 새 `shared/constellation-stages.js`: 계보 ID를 유지하면서 레벨별 그림·능력을 반환한다. Lv5·초월체는 자료가 없어 Lv4를 임시 사용하고 내 정보에 안내한다.
- `client/world.js`, `client/app.js`: 실제 본인/친구 맵 캐릭터, 초상화, 원본 카드, 능력 제목·설명을 레벨에 맞춰 표시한다. 기존 이동 보간과 방향키/터치를 그대로 이용한다.
- `server/evolution.js`: 변경 선택16종도 현재 레벨 카드로 보여 준다. 수동 진화·XP0·동일 계보 최대2명 규칙은 유지한다.
- `server/app.js`, `server/constellation-abilities.js`: Lv3/4 물병·염소·황소 보상, Lv3 쌍둥이 가격4 이하 복사, 까마귀 가격 한도 제작, 천칭2/3주사위 및 유료 재시도를 서버에서 계산한다. 주사위 테스트용 함수 주입은 서버 생성자의 내부 옵션이고 네트워크 요청으로 지정할 수 없다.
- `client/temple-ui.js`: 교사 확인 기록에는 사용 당시 레벨을 남긴다. Lv4 천칭을 Lv2로 오인하여 XP를 주지 않도록 구분했다.
- 제작 대기는 획득 당시 가격·레벨 한도를 유지한다. 진화·계보 변경으로 주간 사용 한도를 초기화하지 않는다. 기록 복원은 신규 pending/marker 필드를 검증하며 기존 Lv2 기록도 읽는다.
- 하위 작업이 별도 요청123으로 기록한 자료 추출은 요청122에 병합했다. 자동 좌표 기반 이미지 후보는 반복 그림 오류가 있어 본체가 원본 시각 확인으로 교정했다. 최종 매핑은 docs JSON이며 `.local` 초기 후보는 재사용하지 않는다.

## 4 검증 결과

- `node --test --test-concurrency=1 tests/*.test.js`: **229/229 통과**(신규8 포함).
- `node scripts/verify-browser.mjs`: **91개 통과**, 브라우저 오류0.
- `node scripts/verify-growth-integration.mjs`: **9개 통과**, 기존 Lv1→Lv2·성장·주사위 회귀 확인.
- `node scripts/verify-avatar-stages.mjs`: **7개 통과**, 실제 두 학생 브라우저의 Lv2→3→4 진화·XP0·새 PNG 캔버스 사용·방향키 서버 위치 및 친구 캔버스 위치 변경·주간 제한·390px 4열 선택·32종 디코딩 검사. 임시 메모리 교실만 사용했다.
- PNG32개 모두 RGBA이며 알파 최솟값0/최댓값255. 모든 새 단계 이미지가 Lv2 및 서로 다른 단계와 다른 파일임을 검사했다. 카드32종+변형1종은 PPT ZIP의 원본 이미지 바이트와 일치한다.
- 검증 스크립트 초기 오류 수정: Lv4에서 이미 사용한 주간 능력의 비활성 버튼을 클릭하려던 검사를 금지 상태 확인으로 변경. 진화의 별 내부에 배치한 시험 캐릭터가 충돌로 이동하지 못해 정상 접근 좌표로 수정. 갤러리의 시험 전용 인라인 CSS는 CSP가 없는 별도 검수 페이지에서 적용하며 실제 앱 CSP는 유지했다. 화면 캡처는 비동기 모달이 열린 뒤 수행하도록 수정했다.
- 최종 갤러리32종·실제 능력 창·모바일 선택창을 직접 시각 검수했다. 캐릭터 발/날개와 투명 배경, 카드 단계 구분 확인.
- 로컬 `/health` 정상·지속 저장 활성. 실제 학급 JSON **4개 재시작 전후 SHA256 일치**.
- 공개 `/health`, 공개 코드/PNG **5파일 SHA256 일치**, 인증정보 없이 WebSocket 연결 확인.

## 5 최종 점검(검수)

Astra가 `43b64ca` 이후 변경을 통합 검수했다. Lv2 경로·구형 기록 호환, 서버 권한/주간 제한, 재화 상한·황소 비용 부족·까마귀 가치/중복/종류 제한, 천칭 재시도 중복 방지, 단계별 원본 연결을 확인했다. `.env`, `.local`, 실제 학급 저장자료와 교사 확인키는 커밋하지 않는다. 사냥 코드는 변경하지 않았다.

## 6 미완료 사항과 다음 단계

- 구현 범위: 32종 외형·진화/이동·카드 설명·계산 가능한 능력 자동 처리 완료.
- 합성, 뱀 소환, 친구 동의, 현실 수업 판정 등 복잡한 능력은 선생님 확인 요청으로 남긴다. 사용자는 선택 질문에 아직 답하지 않았으며 본체가 이 기준을 안내하고 진행했다. 주간 규칙은 기존 규칙 유지다. 별 카드/뽑기 카드는 선생님이 별도 지급 후 처리 완료한다. 처리 완료 버튼이 모든 보상을 자동 지급하지 않는다.
- 원본의 Lv4 뱀 소환 수(랜덤1/3)와 물고기 두 역할의 선택·일부 배분량은 추가 운영 규칙이 필요하다. 자동 판정으로 추측하지 않았다.
- Lv5·초월체 전용 그림/능력은 추후 자료가 필요하다. 현재 Lv4 fallback. 몬스터 사냥은 준비 중 유지.
- 최신 운영: 실행기 **PID27476**, 로컬 `http://127.0.0.1:3000/`, 공개 **https://grades-african-love-organ.trycloudflare.com/**. 이전 ten-contractor 주소는 종료. 학생에게 새 주소에서 메뉴의 학생 입장 링크를 다시 배부한다. 학생 시간07~21시 유지.
- 실행 로그/검사 결과는 비공개 `.local/122-*`, 갤러리 원본은 `.local/avatar-stages-gallery.png`. 다음 새 요청은 **123**.

## 7 Git 동기화 결과

시작 기준 `43b64ca`에서 최신 상태를 확인했다. 하위 작업 중간 커밋 `9b6a30b`(표시)·`a23b520`(추출 기록)은 되돌리거나 강제 push하지 않고 후속 통합 커밋으로 완성한다. 기능 통합 커밋 **`ab49d527ef0e9c5459dddbe5e36761b8bf3ede96`**을 `origin/main`에 push했고 로컬 HEAD·origin/main·`git ls-remote`의 실제 main SHA가 모두 일치함을 확인했다. 비밀/실제 학급 자료는 제외했다. 이 확인 문서와 테스트 파일의 불필요한 import·끝 빈 줄 정리는 후속 문서 커밋으로 저장한다. 기능 검증8개 재실행도 통과했다. 최종 문서 커밋 후에도 push·세 SHA 일치·깨끗한 작업 트리를 확인한다.
