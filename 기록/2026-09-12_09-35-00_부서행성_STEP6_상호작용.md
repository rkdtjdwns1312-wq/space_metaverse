# 부서행성(STEP 6) 오브젝트 상호작용

- 작업 시각: 2026-09-12 09:35 KST 시작, 11:10 KST 완료
- 작업 도구: Claude Code. 계획·최종 점검 Claude Fable 5.1, 구현 Claude Sonnet 서브에이전트 3개(서버 A·화면 B 병렬, 통합 C), 검증 Claude Opus 서브에이전트 1개. Astra 검수 아님.
- 관련 요청: 023
- 상태: 구현·자동 검증 완료, **사용자 화면 확인 대기**

## 1. 요청 요약

- 사용자(2026-09-12 09:3x KST): 행성마다 넣고 싶은 반응은 크게 없음. 아이들이 움직이는 맵은 우주 공간이고 행성은 하나의 부서. 아이들은 교실에서 맡을 역할에 따라 행성에 가입하고, 가입된 곳만 들어갈 수 있음. 행성을 눌렀을 때 소속된 아이들에게만 '입장' 버튼이 생기고, '행성의 규칙 확인하기'를 누르면 행성에서 정한 교실 규칙(예: 줄 안 서면 경고)이 나오면 좋겠음.
- 원래 요구사항(요청 012 기록): 부서행성은 아지트 개념, 한 아바타는 한 부서만 가입, 자유 가입/탈퇴, 가입자만 출입. 근처 오브젝트에 E키 안내와 상호작용 버튼, 모바일 터치 버튼. 권한은 서버 검증.
- **추가 지시(2026-09-12 09:5x KST, 구현 도중)**: 일기·독서·교과·청소행성은 예시 이름일 뿐이다. 각 행성은 그런 느낌의 역할 행성이고, 이름은 아이들이 다수결로 수정할 수 있다. 아이들이 만들어내는 교실의 역할이 곧 행성이며 개수는 7개도 10개도 될 수 있다. 아이들이 직접 만들 수 있게 하고, 선생님은 행성을 만들 때 승인만 한다. 행성을 만들 위치는 우주 맵에서 아이들이 직접 지정한다.

## 2. 계획 (Fable)

- 1차 설계(고정 행성 4개)로 서버·화면 에이전트를 시작한 직후 추가 지시를 받아 **동적 행성 설계로 변경**했다. 1차 구현 중 살아남는 부분: 내부 맵, 가입/탈퇴/입장/나가기, 규칙, 상호작용 프롬프트, 대화상자 골격. 바뀌는 부분: 행성이 설정 상수가 아니라 방별 데이터가 되고, 신청·승인·위치 지정·이름 투표가 추가된다.
- 설계 결정(최종):
  - 행성은 방(교실)마다 동적으로 만들어지는 데이터. 방을 만들 때는 행성이 없고 가운데 '모여라, 빛나는 별'만 있다. 선생님이 교실 만들기에서 '예시 행성 4개로 시작'을 켜면 독서·일기·청소·교과 예시가 미리 놓인다(기본 꺼짐).
  - 만들기: 학생이 '행성 만들기' → 지도에서 자리를 클릭 → 이름(2~10자)·소개(40자 이내)·색(팔레트 8색) 입력 → 신청. 서버가 경계·겹침(별·기존 행성·대기 중 신청과 간격 30px 이상)·금지어·개수 한도(방당 12개, 대기 6개, 학생당 대기 1개)를 검사. 선생님이 '승인' 또는 '돌려보내기'. 승인되면 신청한 학생이 첫 멤버가 된다. 선생님이 직접 만들면 즉시 생성. 선생님은 행성을 없앨 수도 있다(안에 있는 사람은 광장으로, 소속은 해제).
  - 이름 바꾸기: 소속 학생이 새 이름을 제안하면 소속 학생 전원이 찬성/반대 투표(공개 투표, 바꿀 수 있음). 찬성이 소속 인원의 과반(⌊n/2⌋+1)이 되면 즉시 변경, 반대가 과반이거나 통과가 불가능해지면 부결. 행성마다 한 번에 하나의 투표. 선생님은 투표 없이 바로 바꿀 수 있다. 소속 1명이면 제안이 곧 통과.
  - 행성 내부 맵(`planet:<id>`, 광장과 같은 1200×760, id는 UUID라 이름을 바꿔도 유지)에 규칙 게시판(충돌)과 광장으로 나가는 문(통과 가능). 같은 맵에 있는 사람만 서로 보임.
  - 학생은 한 행성에만 소속. 다른 행성 가입은 자동 옮기기('옮겨오기'). 탈퇴 가능. 선생님은 가입 없이 모든 행성 입장.
  - 입장 조건은 서버 검사: 광장에 있음 → 소속 또는 교사 → 행성 가까이(행성 반지름 + 소행성 반지름 + 40px). 나가기는 내부 어디서든 가능.
  - 규칙: 새 행성은 기본 규칙 '서로 존중하고 친절하게 말해요' 한 줄로 시작. 선생님이 1~8줄·40자로 수정. 예시 행성은 각자 예시 규칙(청소행성 '줄을 서지 않으면 경고를 받아요' 등).
  - 신청·승인·거절·삭제·가입·탈퇴·옮기기·이름 투표 시작·결과·규칙 변경은 시스템 채팅 안내. 입장·나가기는 안내 없음.
  - 위치 브로드캐스트는 전원에게 보내고 화면이 mapId로 걸러 그림. 대기 중 신청은 지도에 점선 원으로 표시.
- 작업 분할: Fable이 `shared/config.js` 계약 작성 → Sonnet A(서버·테스트)와 Sonnet B(화면·README) 병렬 → 추가 지시 반영을 같은 에이전트에 이어서 지시(맥락 유지) → Sonnet C(브라우저 통합 검사·연동 수정) → Opus 검증 → Fable 최종 점검·커밋.

## 3. 수행 내용과 변경 파일

- `shared/config.js`(Fable 작성, 2회): `INTERACT`, `DEPARTMENT_RULES`, `PLANET`(반지름 60, 간격 30, 방당 12개, 대기 6개, 이름 2~10자, 소개 40자, 기본 규칙, 예약 구역 `reserved`), `PLANET_COLORS` 8색, `MAP`(별만), `PLAZA_ID`, `EXAMPLE_PLANETS` 4개, `INTERIOR` 템플릿, `interiorIdOf`, `planetIdOfMap`, `mapOf(mapId, planets)`.
- 서버(Sonnet A, 2회): `server/world.js`(맵별 `isFree`, `spawnInside`, `exitPosition`, `isNear`, `placementFree`, `addPlanet`(생성 자리의 플레이어 밀어내기)), `server/rooms.js`(`room.planets`/`room.proposals`, `seedPlanets`, 스냅샷 `planets`/`proposals`/`mapId`/`departmentId`, `renameView`), `server/app.js`(액션 14개: propose/create/approve/reject/withdraw/remove/join/leave/enter/exit/rules:set/rename:propose/rename:vote/rename:set, `planetInput` 검증, `evaluateRename` 과반 판정, 퇴장·만료 시 재평가, 받침에 따른 조사 `ro()`), `tests/engine.test.js`·`tests/realtime.test.js`(신규 29개).
- 화면(Sonnet B, 2회): `client/world.js`(동적 행성·신청 점선 원·배치 미리보기·내부 맵 게시판/문·`nearby`/`planetAt`/`canvasPoint`), `client/app.js`(E 프롬프트, 행성 대화상자(가입/옮기기/탈퇴/입장/규칙/이름 투표/교사 즉시 변경·삭제), 행성 만들기 배치 모드와 신청 대화상자, 신청 패널(승인/돌려보내기/취소), 여권 소속 표시, 예시 행성 체크박스), `client/index.html`, `client/style.css`, `README.md` 7-2절.
- 통합(Sonnet C): 광장에서 행성 클릭으로 대화상자 열기(멀리서도 관리), 내부 맵 '우리 행성 정보' 버튼, 중복 `showModal` 가드. `scripts/verify-browser.mjs`에 STEP 6 검사 30개 추가(`walkNear`가 서버 좌표를 읽어 방향키를 조절).
- Opus 직접 수정 4건: 휴대폰(390px) 화면에서 `object-fit:contain` 여백 때문에 캔버스 클릭 좌표가 어긋나 행성을 누를 수 없던 버그(`canvasPoint` 보정, 390px 탭 검사 추가), `reset()`에서 '우리 행성 정보' 버튼 미숨김, 규칙 게시판 8줄·40자 넘침(판 크기 자동 조절), 조사 오류("급식별으로" → 받침 판정). 테스트 1개 추가.
- Fable 직접 수정(Opus 권고 반영): 예약 구역(`PLANET.reserved`, 왼쪽 아래 이동 버튼 자리 240×120)에는 행성 생성 거부 + 배치 모드에서 빗금 표시, 배치 미리보기가 서버와 같은 규칙으로 안 되는 자리를 빨간 점선과 '여기는 안 돼요'로 표시, 가장자리 행성 이름표 잘림 방지, 내부 맵 캡션을 문과 겹치지 않게 위로 이동, 새 신청이 오면 선생님에게 토스트 알림, 투표 문구를 '우리 행성 친구 N명이 찬성하면 바뀌어요'로, 교사 전용 액션의 권한 검사를 행성 조회보다 먼저 하도록 순서 변경, 예약 구역 단위 테스트, README 보강. 처음 예약 구역을 y 600부터로 잡았다가 예시 청소행성(190,565)과 겹쳐 테스트가 실패해 y 640부터로 조정했다.

## 4. 검증 결과 (Opus)

- 종합: 조건부 통과 → 권고 반영 후 Fable 재검증에서 통과.
- 실행: 단위 53/53(수정 후 54/54), 브라우저 51 checks(수정 후 52), errors 빈 배열, 약 106초. 스크린샷에서 행성·고리·이름·소속 인원 정상.
- 서버: 교사 액션은 세션 role로만 판정, 학생은 자기 신청만 취소, 다른 방 id 조회 불가, `planet:enter`는 광장 → 소속/교사 → 거리 순으로 서버 좌표만 사용, 경계·간격·NaN/Infinity/문자열·색·금지어·제어문자·Cf·한도 검사, 갇힘 방지·삭제 시 복귀, 스냅샷에 비밀 필드 없음, `mapOf` 비용 무시할 수준.
- 클라이언트: innerHTML 0건, 인라인 style 속성 0건, 대화상자 자동 닫힘 정상. 390px 좌표 버그와 reset 누락은 수정.
- 테스트 품질·README·범위·회귀 통과. UX 권고 9건 중 7건 반영(3절), 미반영 2건은 6절.

## 5. 최종 점검 (Fable)

- 권고 반영 후 재실행: `node --check` 통과, 단위 54/54, 브라우저 52 checks·errors 빈 배열·105초. `.local/05-planets.png`(승인 직후 교사 화면)를 직접 확인.
- 사용 모델: 계획·수정·최종 점검 Fable, 구현 Sonnet 3개(A·B는 설계 변경 후 같은 에이전트에 이어서 지시), 검증 Opus 1개.

## 6. 미완료 사항과 다음 단계

- **사용자 확인(STEP 6)**: 절차는 아래 보고와 같다. 예시 행성 없이 시작해 학생이 행성을 신청하고 선생님이 승인하는 흐름을 직접 해 본다.
- 미반영 권고(설계 판단 필요): 대기 신청에 만료 시간이 없어 선생님이 방치하면 그 학생은 취소 전까지 새 신청 불가(현재는 학생이 '취소' 가능). 재접속 대기 중(최대 60초)인 소속 학생도 투표 정족수에 포함됨.
- 확인되면 STEP 7 아이템(인벤토리·별 파편, 상점은 이후)으로 진행. 순서: STEP 7 아이템 → STEP 8 교사 관리 패널.

## 7. Git 동기화 결과

- WIP 커밋 `8c2fcac`(Opus 검증 전) push 완료.
- 최종 커밋(Opus·Fable 수정, 검사 52개, 이 기록, 메인.md)은 이 기록 작성 직후 push하며 해시·원격 일치는 `메인.md` 현재 상태 절의 요청 023 항목에 기록한다.
