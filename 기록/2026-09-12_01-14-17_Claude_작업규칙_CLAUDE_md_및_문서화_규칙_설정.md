# Claude 작업 규칙(CLAUDE.md) 및 문서화 규칙 설정

- 작업 시각: 2026-09-12 01:14 KST 시작
- 작업 도구: Claude Code (본체 모델 Claude Fable 5.1)
- 관련 요청: 016, 017, 018

## 1. 요청 요약

- 요청 016: 앞으로 Claude Code로 작업할 때는 Fable로 계획을 세우고, Sonnet으로 작업하며(독립 작업은 여러 에이전트 병렬 허용), Opus로 검증한 뒤, 마지막으로 Fable로 최종 점검하는 4단계 순서를 적용한다. 이 내용을 `CLAUDE.md`로 남긴다.
- 요청 017: 앞으로 작업하는 데 중요한 내용을 md로 다 남긴다.
- 요청 018: 작은 작업에도 전체 파이프라인을 적용하니 시간이 오래 걸린다는 사용자 지적에 따라, 소규모 문서·규칙 작업은 Fable 본체가 직접 처리하고 코드 구현·다중 파일 개발 작업에만 전체 파이프라인을 적용하기로 사용자가 결정했다. 이번 작업(016·017)의 검증도 Opus 대신 Fable이 직접 수행한다.

## 2. 계획 (Fable)

- 이 프로젝트는 Codex(gpt 계열) 에이전트가 만든 문서 체계(`AGENTS.md`, `메인.md`, `기록/`)를 이미 갖추고 있으므로, Claude Code용 새 규칙은 별도 파일(`CLAUDE.md`)로 분리하고 기존 체계와의 관계를 명시하는 방향으로 정한다.
- 산출물을 3가지로 나눈다: (1) `CLAUDE.md` 신규 작성/재작성, (2) `메인.md` 갱신(모델 규칙 하위 절, 규칙 목록 추가, 요청 이력 016·017 행 추가, 현재 상태 갱신), (3) 이번 작업 자체를 남기는 기록 파일 신규 작성.
- 세 산출물은 서로 의존성이 낮아 Sonnet 서브에이전트 2개에 병렬로 위임한다. 에이전트 A가 (1) `CLAUDE.md`를, 에이전트 B가 (2) `메인.md` 갱신과 (3) 이 기록 파일을 맡는다. 본체(Fable)는 계획 수립과 최종 통합·점검을 맡는다.
- 검증 단계에서는 Opus 서브에이전트가 메인.md 표 무결성(빈 줄 제거, 신규 행 형식), 기록 파일의 7절 구성 준수, 시각·모델 표기의 정확성을 점검하도록 지시한다.

## 3. 수행 내용과 변경 파일 (Sonnet 서브에이전트 2개)

- `CLAUDE.md` (에이전트 A, 재작성): 7개 절로 구성했다. 1 문서 관계(`CLAUDE.md`·`AGENTS.md`·`메인.md`·`기록/`·`README.md`의 역할과 우선순위), 2 작업 시작 전 필수 절차 4단계, 3 모델 파이프라인 4단계와 Codex 규칙 대응, 4 문서화 규칙(남길 항목·저장 위치·기록 파일 표준 구성·시점·금지), 5 Git 동기화, 6 사용자 소통 방식, 7 현재 프로젝트 맥락.
- `메인.md` (에이전트 B, 직접 Edit 도구로 수정):
  - 4번째 줄 '최근 갱신' 시각을 2026-09-12 01:17:44 KST로 갱신.
  - '합의한 작업 규칙' 목록 끝에 요청 017 문서화 규칙 항목 추가.
  - '모델 선택과 하위 에이전트 위임' 절, '초보자인 사용자를 위한 진행 방식(요청 013)' 절 뒤에 '### Claude Code 세션의 모델 규칙 (요청 016)' 하위 절 신설. Fable→Sonnet→Opus→Fable 파이프라인, Codex 규칙 대응표(gpt-5.6-luna↔Sonnet, Astra↔Opus 검증+Fable 최종 점검), Astra 미사용 명시, 적용 예외(단답형 질문)를 기재.
  - 요청 이력 표: 011~015행 사이에 끼어 있던 빈 줄들을 제거해 하나의 표로 통합(행 내용 변경 없음). 표 끝에 016·017행 추가.
  - '현재 상태와 미완료 사항' 절, '요청 010·011' 항목 앞에 요청 016·017 항목 추가: CLAUDE.md 반영 완료 사실과 git 명령 미인식으로 인한 이번 변경분 commit/push 미완료 사실을 기재.
- `기록/2026-09-12_01-14-17_Claude_작업규칙_CLAUDE_md_및_문서화_규칙_설정.md` (에이전트 B, 이 파일): 신규 작성.
- `AGENTS.md`는 Codex 세션용이므로 이번 작업에서 수정하지 않았다. 코드 파일도 변경하지 않았다.
- Claude 프로젝트 메모리: 요청 016 처리 초기에 4단계 파이프라인을 메모리에도 저장했으나, `CLAUDE.md`가 세션마다 자동 로드되어 중복되므로 메모리 항목을 삭제하고 `CLAUDE.md`로 일원화했다.
- 요청 018 반영 (Fable 직접 처리): `CLAUDE.md` 3절에 '적용 범위' 하위 절 추가, 기록 파일 표준 구성에 직접 처리 시 표기 방법 추가. `메인.md`의 Claude Code 모델 규칙 적용 범위 항목 수정, 요청 이력 018행 추가, 현재 상태 항목 갱신. 이 기록 파일의 1·3·4·5절 갱신.

## 4. 검증 결과 (Fable 직접 검토 — Opus 미사용)

- Opus 검증 에이전트를 시작했으나 소요 시간 문제로 사용자가 중단했고, 요청 018 결정에 따라 Fable 본체가 직접 검토했다. Astra 검수나 Opus 검증이 아니다.
- `CLAUDE.md`: 계획한 7개 절이 모두 있고 명세 항목(문서 관계, 시작 전 절차 4단계, 파이프라인 4단계와 Codex 대응, 문서화 규칙, Git, 소통 방식, 프로젝트 맥락)이 누락 없이 반영됐다. 루트에 `README.md`가 없다는 서술은 실제와 일치한다(Glob으로 확인).
- `메인.md`: 요청 이력이 하나의 표로 통합됐고 모든 행의 열이 4개로 일치한다. 016·017 행의 상세 기록 링크가 이 파일 이름과 정확히 같다. 기존 001~015 행과 규칙 문단은 빈 줄 제거 외 변경이 없다.
- 이 기록 파일: 7절 구성을 지켰다.
- 세 파일 사이에 파이프라인·Astra 대응·git 상태 서술의 모순이 없다. 비밀값은 포함되지 않았다.
- 발견된 오류: 없음. 수정 사항: 요청 018 반영만 추가했다(3절 마지막 항목 참고).

## 5. 최종 점검 (Fable)

- 산출물 3개(`CLAUDE.md`, `메인.md`, 이 기록 파일)를 직접 확인하고 요청 018 반영을 마쳤다.
- 사용 모델: 계획·검토·최종 점검은 Claude Fable 5.1, 작업은 Claude Sonnet 서브에이전트 2개, Opus는 사용하지 않았다.

## 6. 미완료 사항과 다음 단계

- git 명령이 이번 Claude Code PowerShell 세션 PATH에서 인식되지 않아, 이번 작업(CLAUDE.md 재작성, 메인.md 갱신, 이 기록 파일 신규 작성)의 commit·push를 수행하지 못했다. 다음 작업자는 `C:\Program Files\Git\cmd\git.exe` 등 일반 설치 경로를 확인하거나 PATH 갱신 후 `git pull --ff-only origin main` → 변경분 commit → `git push origin main` → 원격 HEAD 일치 확인을 수행한다.
- `AGENTS.md`는 Codex 세션용으로 그대로 유지한다. `CLAUDE.md`와의 관계(우선순위, 역할 분리)는 `CLAUDE.md` 1절 '문서 관계'에 명시되어 있다.
- 요청 014·015(STEP 3~4 브라우저 검증 오류 수정, README 작성, 최종 검수)는 여전히 진행 중이며 이번 작업에서는 다루지 않았다. 다음 작업자는 `기록/2026-09-12_01-08-50_클로드_기본엔진_후속작업_지시서.md`를 참고해 이어간다.

## 7. Git 동기화 결과

- 미완료. 원인: Claude Code PowerShell 세션에서 `git` 명령이 인식되지 않음(PATH에 없음, 2026-09-12 01:14 KST 확인). 강제 실행이나 우회 시도는 하지 않았다. pull/commit/push 모두 수행하지 못했으므로 메인.md '현재 상태와 미완료 사항'에 미완료로 기재했다.
