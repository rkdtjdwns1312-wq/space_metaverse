# 요청224~225 별피하기 개선·생명의별

- 작업 도구: Codex
- 사용 모델: gpt-6-astra(Astra) 본체 구현·통합. 224 서버/검사 하위 Astra는 변경 후 사용량 한도로 종료되어 본체가 직접 확인·검증 완료.
- 최종 검수 모델: gpt-6-astra(Astra), 직접 검수 PASS.

## 1. 요청 요약

224: 별피하기 난이도 증가를 5초에서4초로, 움직임을 부드럽게.
225: 광장 가장 중앙에 작은 흰 원형 생명의별, 아래 이름 표시. F/터치 조사로 체력과 마나를10초에 걸쳐 가득 회복.

## 2. 계획

별피하기 서버 규칙과 판정은 유지하면서 위치 전송을50ms로, 화면은75ms 버퍼로 서버 확정 좌표 사이를 매 프레임 연결한다. 생명의별은 신전 바닥의 실제 원형 중심에 배치하고 기존 상호작용/HP·MP 표시를 연결한다. 회복은 서버 시간으로 계산하며 일시적인 전투 상태로만 보관한다.

## 3. 수행 내용과 변경 파일

- server/dodge-game.js: waveMs4000, broadcastMs50, 실제 서버 시각+0.5ms 타이머 허용 오차로 전송. 지연 시 물리 계산50ms 제한은 유지.
- client/motion.js: 보간 시간 선택값 추가, 기존 월드 기본150/100ms 유지.
- client/dodge-game.js: 플레이어/날아오는 별에50ms/75ms 보간, 종료/재시작 시 실제 충돌 좌표로 초기화. UI 안내4초. 서버 충돌·생존시간·주간10위 규칙 유지.
- tests/dodge-game.test.js·tests/motion.test.js·scripts/verify-dodge.mjs:4/8초 경계,50ms와49.999ms 전송,60/120Hz 보간, 실제 브라우저 중간 좌표·키보드·터치·정리 검증.
- shared/config.js: 생명의별(x1080,y684,r26). 신전 중심700에서 바닥 그림이16px 위이므로 그 중심에 맞춤. 길을 막지 않음.
- client/life-star-art.js: 흰 구체·민트빛 후광·가는 고리·작은 반짝임. 모션 줄이기 설정 준수. client/world.js 근처 대상/이름, client/universe-ui.js 작은 지도 모양.
- server/life-star.js:10초·1초10단계 정수 회복. 남은 부족량을 남은 횟수로 분배하며10초째 최대치. 반복 조사는 중첩/초기화하지 않음. 시작 때 서버가 접속·광장·거리·생존 확인. 클라이언트의 시간/회복량/대상은 받지 않음.
- server/app.js·client/app.js:life-star:recover, HP/MP 업데이트 및 완료 안내. 같은 맵에 현재치 전달. 시작 후 이동해도 회복 지속, 접속 종료/사망/레벨 변경은 기존 회복 취소. 재화 저장 형식 변경 없음.
- tests/life-star.test.js·scripts/verify-life-star.mjs:개인별 회복·권한·거리·위조·취소·실제 F/터치 및 HP/MP 표시 검증.
- tests/engine.test.js:첫 물체가 장애물이라는 오래된 순서 의존을 제거하고 실제 알림장 기둥을 지정하여 충돌 검증.

## 4. 검증 결과

- 전체509/509 PASS, 기본/지속저장 브라우저91건 PASS(errors[]).
- 별피하기 브라우저9건 PASS. 4/8초 경계와50ms 전송, 부드러운 중간 좌표·방향 전환·충돌 정지 검사 통과.
- 생명의별 단위/실제소켓8건 PASS. 실제 브라우저3묶음: F 시작,5초 절반/10초 최대치, 반복 눌러도 시작시간 유지,390px 터치와 이미 가득한 경우 안내 PASS.
- 검증 중 교사MP는 기존40이므로 잘못된 테스트 기대값99999를40으로 수정. 실제 능력치 변경 없음.
- 새 생명의별을 첫 오브젝트로 등록하며 기존 장애물 검사가 실패하여 실제 기둥 지정으로 원인 수정. x/y 모두 같은 기둥으로 지정 후 전체 재검증 PASS.
- Astra가 PC/모바일 생명의별 화면을 직접 확인. 바닥 중심과 작은 흰 구체·아래 이름·조사 안내 확인.
- .local/225-all-tests.log,225-base-browser.log,225-browser.log,224-browser.log에 검증 근거 보관(비공개).

## 5. 최종 점검(검수)

Astra 승인. 학생 HP/MP는 서버가 결정하고 회복 중첩·임의 즉시 회복 불가. 재화·인벤토리·진화·사냥 규칙은 유지. 정적 그림과 시간 회복 모듈 분리. 이전 요청220~223도 별도 기록의 검증을 마쳤다.

## 6. 미완료 사항과 다음 단계

225 운영 서버PID12932, https://sally-thanksgiving-thumbnails-con.trycloudflare.com. 학생 배부는 교사 메뉴→학생 입장 링크 복사(교실 코드 포함). PC/서버가 켜져 있어야 함. 기존 법칙대로 재시작 시 임시 주소 변경. 실제 교사 확인키를 공개 검사에 사용하지 않음. 외부 공개는 요청219에서 받은 명시 승인에 따라 유지했다.

요청224 중간 운영 주소 laws-africa-processors-kind는225 반영 시 종료. 최신 주소는 위 주소. 실제 학급 데이터는 비공개 백업 후 보존 검사. 이전 미정 카드/스킬 효과와 테스트 시간 제한 해제는 계속 기존 상태. 다음 요청226.

## 7. Git 동기화 결과

시작d37902c, pull --ff-only 최신 확인. 검수 후 변경된 소스/검사/문서만 선별 commit/push한다. 사용자 원본 몬스터 디자인/은 미추적 보존. .env/.local/data는 제외. 최종 원격 일치 결과는 동기화 후 후속 기록.
