# 요청 034 — 지속 저장 최종 검증 재개

- 작업 도구: Codex
- 사용 모델: gpt-6-astra(Astra, 본체)
- 검수 모델: gpt-6-astra(Astra, 본체 직접 검수)
- 작업 시각: 2026-09-15 00:49 이후, 한국 시간

## 1. 요청 요약

"하던 작업들 그대로 진행해줘"에 따라 요청 027·033의 지속 저장 구현을 마무리한다. 구현된 기본 엔진을 처음부터 다시 만들지 않는다.

## 2. 계획

기존 변경 보존 및 원격 확인 → 중단된 서버 종료 검사 원인 재현 → 수정·전체 검사 → 기록·Astra 검수 → commit/push.

## 3. 수행 내용과 변경 파일

- 메인 상태와 변경 파일을 다시 읽고 `git pull --ff-only origin main`을 수행했다. Already up to date였으며 기존 변경을 보존했다.
- 이전 통합 검사는 기존 82개 통과 후 신규 저장 검사에서 서버를 종료할 때 멈췄다. 연결만 하고 HTTP 요청을 보내지 않는 `net.connect()`로 종료 대기를 재현했다. 브라우저가 미리 열어 둔 빈 TCP 연결이 원인이었다.
- `server/app.js`: 받은 TCP 연결을 추적하고 저장 후 서버를 종료할 때 남은 연결까지 닫는다. 종료 시 저장 오류는 호출자에게 돌려주고 파일 잠금을 해제한다. 저장 검사를 생략하거나 브라우저를 먼저 닫는 식으로 우회하지 않았다.
- `tests/persistence.test.js`: 빈 TCP 연결이 있는 상태에서 종료와 같은 저장 폴더 재시작이 완료되는 회귀 검사 추가.
- `scripts/verify-browser.mjs`: 앞선 작업에서 진행 로그·화면 대기 제한·실패 보고서를 추가했다. 기존 82개와 신규 저장 7개 검사를 함께 실행한다.
- `AGENTS.md`, `메인.md`, 이전 구현 기록: 최종 검사 수와 후속 작업·인계 문서를 갱신한다.
- 일회성 재개 예약 `automation`은 원래 지시대로 **PAUSED** 처리했다. 사용량 조회만 수행했고 초기화 크레딧은 사용하지 않았다. 예약 정리에 OpenAI Docs 지침과 [공식 문서](https://learn.chatgpt.com/docs/automations?surface=app)를 참고했다.
- 9월 14일 재개 시도는 자동 승인 검토가 사용량 한도를 이유로 Git pull을 거절해 중단됐다. 9월 15일 사용량 초기화를 확인하고 정상 승인된 pull로 진행했다. 거절을 다른 경로로 우회하지 않았다.

이번 재개는 앞서 끝난 위임 결과의 통합·검수라 새 하위 에이전트를 쓰지 않았다. 최초 구현의 gpt-5.6-luna(low) 위임 범위는 9월 13일 기록에 있다.

## 4. 검증 결과

- `node --test tests/*.test.js`: **95개 통과**, 실패 0(기존 81 + 파일 저장 5 + 지속 저장/종료 9).
- `node scripts/verify-browser.mjs`: **89개 통과**, `errors: []`, 정상 종료(기존 82 + 실제 저장·서버 재시작·재입장 7).
- 같은 코드·학생 id·소속·행성 규칙·가방·별 파편·최근 공개/개인 채팅 복원과 교사 비밀번호 변경을 확인했다.
- `git diff --check` 오류 없음. `.env`, `.local/`, `data/`는 커밋 제외. `교실-시작.cmd`는 변경하지 않아 CRLF를 유지한다.

## 5. 최종 점검(검수)

**Astra 검수 완료.** 저장 필드/비밀번호 해시/교사 권한, 학생별 자산 비공개, 쓰기 실패 시 원본과 메모리 변경 취소, PIN 잠금/변경, 수업 밖 학생 소속 유지, 손상 파일/중복 서버 차단, 정상·강제 종료 복원과 한국어 안내를 확인했다. 테스트 서버와 브라우저는 종료됐다. 실제 운영 교실 데이터는 테스트하지 않았다.

## 6. 미완료 사항과 다음 단계

로컬 지속 저장은 완료했고 사용자 화면 확인을 기다린다. `교실-시작.cmd` → 처음 한 번 교실 만들기 → 학생 닉네임/4자리 비밀번호로 입장 → 소속/소유물 만들기 → 수업 마치기 → 서버 창 Ctrl+C → 다시 실행 → 기존 교실 열기 → 같은 학생 재입장. README 7-0절에 순서가 있다.

온라인 영구 저장소, 자동 백업, 학년 초기화·교실 완전 삭제는 후속 범위다. 데이터는 교사 PC에만 있고 Git에 없으므로 정상 종료 후 `data/classes`를 정기 백업한다. 다음 요청 번호 **035**, 사용자 확인 후 STEP 8 교사 관리 나머지를 진행한다.

## 7. Git 동기화 결과

작업 전 pull 정상 완료. 구현 커밋 `549c2ef`를 `origin/main`에 push했고 2026-09-15 00:56 KST에 HEAD와 origin/main이 `549c2ef2b707dc005459e1f0d0604285bb6740be`로 일치하고 작업 트리가 깨끗함을 확인했다. 이 동기화 결과 기록도 별도 문서 커밋으로 push한다. 사용자 데이터·비밀 파일은 포함하지 않았다.
