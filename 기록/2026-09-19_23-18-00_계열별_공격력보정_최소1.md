# 계열별 공격력 보정·최소1 (요청150)

- 작업 도구: Codex
- 구현·최종 검수: gpt-6-astra (Astra)
- 하위 위임: gpt-5.6-luna / low (Kant) — 공격력 단위검사·조작/몬스터 브라우저 검사 3파일 갱신
- 기록 시각: 2026-09-19 23:18 KST

## 1. 요청 요약

같은 레벨 기본 공격력에서 수호계−2, 제작/생산계−1, 공격계+1을 적용하고 최종 공격력은 최소1로 한다. 언급되지 않은 특수계는 기존 기본값을 유지한다.

## 2. 계획

공격력 계산을 공통 모듈 한 곳에서 처리하고 내 정보와 실제 서버 공격에 모두 적용한다. 전 별자리·단계의 표시 및 실제 피해와 입력 위조 방지를 검사한다.

## 3. 수행 내용과 변경 파일

- `shared/combat.js`: 기본 공격력1/2/3/4와 계열 보정을 분리. `attackPowerOf(level,constellationId)`는 `max(1, 기본+보정)` 반환.
- `server/app.js`, `server/rooms.js`: 클라이언트 입력이 아닌 서버 아바타의 레벨/계보로 실제 피해·타격 표시·내 정보 공격력 계산.
- `tests/hunting.test.js`: 16종×LV2~5 총64조합 실제 소켓 공격에서 피해·HP 감소·타격 이벤트·내 정보 일치 및 공격력/계열/레벨 위조 무시 검증.
- `tests/combat-config.test.js`: 16종 고정 기대표와 단계 오류 검사.
- `scripts/verify-combat-controls.mjs`: 5계열×4단계 내 정보 공격력20값 검사.
- `scripts/verify-monsters-planets.mjs`: 제작계 실제 키보드/터치 피해와 처치 검증을 새 수치에 맞춰 갱신.
- `README.md`: 새 공격력 표와 계산 규칙 기록. 방어/체력은 기존값 유지.

| 계열 | LV2 | LV3 | LV4 | LV5 초월체 |
| --- | ---: | ---: | ---: | ---: |
| 수호계 | 1 | 1 | 1 | 2 |
| 제작계 | 1 | 1 | 2 | 3 |
| 생산계 | 1 | 1 | 2 | 3 |
| 공격계 | 2 | 3 | 4 | 5 |
| 특수계 | 1 | 2 | 3 | 4 |

## 4. 검증 결과

- 서버 전체257/257 PASS. 기본/지속저장 브라우저91건 PASS·오류0.
- 기존 반격/회복 브라우저9건 PASS. 다른 공격자가 남을 때의 추적·이탈3회 회복 유지.
- 새 소켓64조합 검사 통과. LV1은 기존처럼 공격 사용 전 진화 필요.
- 조작 브라우저9건(공격력 표시20값 포함)·몬스터 브라우저10건 PASS·오류0. 실제 제작계LV4 공격2(20→18→16), LV5 공격3(40→37→34), 터치/처치 확인. 하위 검사 결과 JSON의 시각/내용과 최종 코드 일치 직접 검수.
- 운영 PID18616, 로컬3000, 새 공개 주소 `https://monday-accounts-absent-breeding.trycloudflare.com`. 공개33파일 해시 일치·WebSocket·지속저장 health 정상. 실제 학급4파일 SHA256 불변 확인.

## 5. 최종 점검(검수)

Astra가 두 서버 호출 경로와 계산식, 실제 소켓 결과를 직접 확인했다. 하위 초안의 잘못된 공격계/제작계 기대값을 발견하여 고정 기대표를 정정하고 전체257건을 다시 통과시켰다. 실제 제품 계산식은 변경 없이 올바른 값이었다. 검사 기대값을 구현 함수로 계산하여 오류를 숨기지 않았다.

## 6. 미완료 사항과 다음 단계

요청 범위의 공격력 변경 구현 완료. 기존 미연결인 W 실제 스킬 효과·마나 소비·사냥 보상은 이번 범위에 포함되지 않는다. 서버 재시작은 기존 학급/재화 자료를 보존한다.

## 7. Git 동기화 결과

작업 시작 `aaa32e87a1167db3af088a27d1fe1ab4c6659b80` 기준 pull --ff-only 최신 확인. 기능/문서 커밋 `f97ad6e55d4a65f32a6149ab690bd9619fd31ff3` push 완료, HEAD=origin/main=실제 원격 main 일치 확인. 이 결과 기록도 후속 문서 커밋으로 push하고 clean을 확인한다.
