# 공격 간격1초 (요청152)

- 작업 도구: Codex
- 구현·최종 검수: gpt-6-astra (Astra)
- 하위 위임: gpt-5.6-luna / low (Lagrange), 조작·몬스터·반격 브라우저 검사 갱신
- 한국 시간: 2026-09-19 23:51:47

## 1. 요청 요약

공격을1초에1회만 사용할 수 있게 한다.

## 2. 계획

서버와 클라이언트가1000ms 상수를 공유하며 Q/터치가 하나의 공격 대기 시간을 사용하도록 한다. W는 기존450ms를 유지한다.

## 3. 수행 내용과 변경 파일

- `shared/combat.js`: 공격1000ms, 별도 SKILL_COOLDOWN_MS450. 타격 표시340ms·사거리·피해 그대로.
- `client/combat-controls.js`: 상수를 가져와 키보드/터치 공통 공격 간격 적용.
- `server/app.js`: 서버1000ms 재검사, 너무 빠른 요청은 ‘공격은1초에 한 번 할 수 있어요.’ 안내. 스킬 간격은 별도 상수 사용.
- `tests/hunting.test.js`:999ms 거부·피해없음·정확히1000ms 허용·계열64조합 간격 갱신.
- `tests/realtime.test.js` 및 브라우저 검사: 이전450ms 기준 대기를 새 공격 간격에 맞춰 변경.
- README·메인·본 기록 갱신.

## 4. 검증 결과

- 전체 서버257/257·기본/저장 브라우저91·조작9·반격9 PASS. 실제 소켓999ms 거부·1000ms 허용 및 W450ms 유지 확인.
- 몬스터 브라우저10건 PASS·오류0. Q 직후 터치HP18유지·1초후터치HP16·처치·LV5 공격까지 확인.
- 초기 전체검사1건은 기존 실시간 테스트의460ms 재공격 기대 때문에 실패하여 해당 대기를1010ms로 수정했다. 몬스터 UI 검사도 맵 이동 직후 남아 있던500ms 대기 때문에 차단되어1050ms로 맞춰 재검사했다. 서버 규칙을 완화하지 않았다.
- 실행 PID31152·로컬3000·공개 `https://kinds-laptop-jason-house.trycloudflare.com`. 공개33파일 해시 일치·WebSocket·지속 저장 health 정상, 실제 학급4파일 SHA256 불변.

## 5. 최종 점검(검수)

Astra가 서버/클라이언트 공통 상수와 Q/터치 공유 여부,999/1000ms 경계 및 W450ms 유지를 직접 검수했다.

## 6. 미완료 사항과 다음 단계

변경 범위는 학생 공격 간격이다. 몬스터별 반격 간격·미구현 W전투 효과·재화 보상은 기존과 같다.

## 7. Git 동기화 결과

시작 `10b5292e2825fae9f12d6bdaa3ac4cb918c1c70f`에서 pull --ff-only 최신 확인. 검증 후 코드·검사·문서를 함께 commit/push하고 원격 main 일치와 clean을 확인한다.
