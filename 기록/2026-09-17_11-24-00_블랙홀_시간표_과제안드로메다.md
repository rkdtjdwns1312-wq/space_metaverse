# 블랙홀·주간 시간표·개인 과제·과제안드로메다

- 작업 도구: Codex
- 구현 모델: GPT-5 계열 본체 및 일부 UI 하위 작업 gpt-5.6-luna
- 최종 검수 모델: gpt-6-astra (Astra)
- 작업 시각: 2026-09-17 11:24 KST

## 1. 요청 요약

요청 083~087. 별의 기원 우상단 블랙홀과 검은 내부, 부서행성 경고 돌덩이, 소속 학생의 직접 경고와 누적 검은별·교사 해제; 월~금·1~6교시 표; 내정보·과제·가방 분리와 개인 과제; 좌상단 과제안드로메다의 최근 3주 과제·완료 명단; 교사 알림장 줄별 과제 표시. 사용자는 경고의 직접 부여와 달력 기준 최근 3주를 명시했다.

## 2. 계획

지도·서버 판정·지속 저장을 함께 연결하고, 교사/학생 권한과 서로 다른 교실의 데이터를 분리한다. 기존 교실 데이터는 누락 필드에 기본값을 넣어 복원한다. 서버 자동 검사, 실제 브라우저 조작, 재시작 복원을 검증한다.

## 3. 수행 내용과 변경 파일

- `shared/config.js`, `client/world.js`, `client/universe-ui.js`: 별의 기원 양쪽 상단에 부서행성 반지름 4배의 블랙홀·과제안드로메다, 암흑 내부와 경고 돌덩이, 검은별 아바타 표시, 지도 경로를 추가했다. 두 대형 공간과 겹치던 예시 행성 좌표를 옮겼다.
- `server/warnings.js`, `server/app.js`, `server/social.js`, `server/persistent-rooms.js`, `server/rooms.js`, `client/warning-ui.js`, `client/index.html`: 소속 학생이 돌덩이에서 친구·이유를 골라 바로 경고하고, 부서 기준 횟수에 도달하면 검은별로 바뀌어 블랙홀로 이동한다. 검은별 출구·친구 호출·뒤늦은 행성 신청 승인으로 빠져나가는 경로를 차단했다. 교사는 검은별 학생과 발생 부서를 보고 상태를 해제한다. 경고·검은별은 재시작 뒤에도 복원된다.
- `server/temple.js`, `client/temple-ui.js`, `client/style.css`: 시간표를 월~금 열·1~6교시 행으로 바꾸고 교사 입력/학생 읽기 권한을 나눴다. 알림장 각 줄의 과제 체크를 저장하고 과제 이력을 만든다. 한국 시간 월요일 시작의 최근 3주를 사용한다. 오래된 과제 기록은 미완료 개인 과제 참조를 남기고 정리한다.
- `server/tasks.js`, `client/assignment-ui.js`, `client/app.js`, `client/social-ui.js`, `client/index.html`: 가방은 인벤토리만 남기고 내정보·과제·가방을 별도 하단 버튼으로 배치했다. 학생은 체크된 알림장 줄을 개인 목록에 가져오고 완료하면 목록에서 지운다. 과제안드로메다는 주별 과제와 이름순 완료 명단을 표시한다. 교사가 알림장을 수정한 뒤 학생의 오래된 화면에서 엉뚱한 과제를 복사하지 못하도록 과제 ID를 확인한다.
- `tests/*`, `scripts/verify-browser.mjs`, `scripts/verify-homework.mjs`, `scripts/verify-warnings.mjs`, `scripts/verify-navigation.mjs`, `scripts/verify-monsters-planets.mjs`: 새 동작과 기존 지도·행성 배치를 반영한 회귀 검사를 보강했다.

## 4. 검증 결과

- 서버 자동 검사 **197/197 통과**.
- 기본 교실 브라우저 검사 **89개**, 경고 브라우저 **4개**, 과제 브라우저 **4개**, 지도·기둥 **15개**, 몬스터·행성 **6개** 통과. 브라우저 오류 없음.
- 과제 개인 목록·완료 명단과 검은별·경고를 각각 서버 재시작 후 복원하는 검사 통과.
- 기존 `data/classes`는 삭제하거나 초기화하지 않았다. 실제 학급 데이터와 별도의 임시 교실로 검사했다.

## 5. 최종 점검(검수)

Astra가 검수해 검은별 상태에서 이전 행성 신청이 승인되면 블랙홀을 빠져나오던 문제, 과제안드로메다 창 닫기 오류, 오래된 알림장 화면의 다른 과제 복사, 과제 이력 상한 누적을 발견했다. 네 문제를 수정하고 재검수했으며, 추가 조치가 필요한 문제는 발견되지 않았다. 서버 회귀 검사와 실제 화면 검사를 재실행했다.

## 6. 미완료 사항과 다음 단계

2026-09-17 11:31 KST: 새 코드로 로컬 서버를 재시작해 `http://127.0.0.1:3000/`의 HTTP 200을 확인했다. 기존 교실 잠금을 복구했고 자료는 삭제하지 않았다.

사용자의 실제 교사·학생 화면 확인은 아직 받지 않았다. 경고를 소속 학생이 바로 부여하는 운영 방식이므로 교사에게는 검은별 명단/해제 권한을 두었고, 추후 악용 방지 정책이 필요하면 별도 요청으로 조정할 수 있다.

## 7. Git 동기화 결과

처음에는 Windows Git 인증 오류 `SEC_E_NO_CREDENTIALS`로 push가 실패했다. 2026-09-17 13:32 KST에 사용자가 Git 로그인을 승인한 뒤 시스템 Git으로 재시도하여 요청 082~087 커밋 3개(`7bcf3fc`·`a6b357d`·`4cf00d7`)를 `origin/main`에 push했다. 확인 당시 로컬 HEAD와 `origin/main`이 `4cf00d7`로 일치했고 작업 트리는 깨끗했다.
