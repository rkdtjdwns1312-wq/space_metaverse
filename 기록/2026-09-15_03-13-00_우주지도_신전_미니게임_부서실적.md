# 우주 지도·신전·미니게임·부서실적 확장 (046~053)

- 작업 도구: Codex
- 사용 모델: gpt-6-astra(본체), gpt-5.6-luna low(제한된 파일 위임 5건)
- 검수 모델: gpt-6-astra(Astra), 본체 직접 검수
- 작업 기준: 2026-09-15 한국 시간, main 시작 HEAD `72d43a8`

## 1. 요청 요약

- 046: 전체 지도 오른쪽 위, 닫기·Esc, 현재 맵 표시, 아래 실시간 미니맵.
- 047~048: 화살표를 하단 메뉴 높이로, 사이에 E 안내, 오른쪽 원형 터치 조이스틱·E. 버튼 초점과 카메라 역행 수정, 이동 속도 두 배.
- 049: 신전 왼쪽 위 알림장, 왼쪽 아래 시간표, 오른쪽 위 활성 아이템, 오른쪽 아래 이번 주 교사 지급 별 파편. 사용자가 집계 대상을 교사 지급량으로 확인했다.
- 050~052: 별의 기원 / 태양이 머무는 낙원 / 오색별빛 쉼터, 다섯 오락기, 아래 은하수계곡의 잔잔한 흐름.
- 053: 문서에서 실적 저장·제출, 외부 행성 제출 표시, 교사 확인 후 부서 보상·실적 초기화. 부원별 0 포함 분배 제안, 전원 확인 후 지급, 결과 5일 보관.

## 2. 계획

기존 Canvas·Socket.IO·파일 지속 저장 구조를 유지했다. 지도와 조작 UI, 기둥, 게임, 부서실적을 별도 모듈로 나누고 서버 권한·위치·금액 검증을 먼저 연결했다. 본체가 통합·브라우저 검증·운영 반영·Git을 맡았다.

## 3. 수행 내용과 변경 파일

- `shared/config.js`: 속도 310→620px/s, 네 기둥·다섯 오락기·은하수계곡과 왕복 문, 내부 실적 문서. GARDEN 맵 조회가 중앙광장으로 잘못 떨어지던 기존 분기도 수정.
- `client/world.js`, `scenery.js`: 아바타와 카메라가 같은 프레임 보간 위치 사용. 파스텔 햇살·다섯 성운·은하수 곡선 배경. 느리게 흐르는 입자, 움직임 줄이기 설정 존중. 제출 행성 상단 배지와 내부 문서 그림.
- `client/universe-ui.js`, `joystick-ui.js`, `index.html`, `style.css`, `app.js`: 전체 지도·현위치·미니맵, 하단 조작, 버튼 초점 복귀. 방향키가 입력란/열린 창에서는 움직이지 않으며 조이스틱 손 떼기·취소·창 전환 시 정지.
- `client/temple-ui.js`, `server/temple.js`: 한국 날짜의 알림장/시간표(교사 작성), 활성 효과, 월요일 기준 실제 지급 합계. 개인 자산/비밀 아이템 발신자를 학생에게 추가 공개하지 않음.
- `client/arcade-ui.js`, `arcade.css`: 짝맞추기·숫자 순서·별 누르기·덧셈·반응 속도. 서버가 오락기 근접 검사. 이번 단계는 1인 연습, 보상·점수 저장 없음. 종료 시 타이머 정리.
- `server/department-work.js`: 공동 초안 version, 제출 잠금, 지급 후 백지, 부서 잔액 상한 9999, 제안당 전원 수량·확인, 마지막 확인 직전 자산 재검사. 한 번만 지급, 구성원 변경 시 취소, 완료 결과 정확히 5일.
- `client/department-work-ui.js`: 실적·분배하기·분배결과 탭, 교사 지급 UI, 0 포함 인원별 입력, 확인 진행 표시. 서버 갱신으로 작성 중 글을 덮어쓰지 않음.
- `server/app.js`, `rooms.js`, `persistent-rooms.js`: `department:get/save/submit/award/propose/confirm/cancel`, 변경 알림. 해당 부서/교사만 내부 데이터 조회, 학생 실적 작성은 내부 문서 근접, 공개 스냅샷은 `reportPending`만 추가. 성공 저장 후 알림, 실패 롤백. 부서 보상은 개인 분배 시 주간 지급량으로 집계. UTF-8 4000자 실적을 위해 소켓 본문 제한 32KiB.
- 테스트·README·메인·AGENTS 문서도 갱신했다. 비밀·실제 학생 자료는 기록하거나 커밋하지 않았다.
- Luna low 위임: Newton(HTML/CSS/조이스틱), Parfit(기둥 순수 로직/단위 검사), Socrates(게임 UI), Nietzsche(부서 순수 로직/단위 검사), Cicero(부서 지속 저장 소켓 검사). 모두 지정 파일만 통합했다. 본체가 최종 오류 수정과 검수를 수행했다.

## 4. 검증 결과

- `node --test tests/*.test.js`: **118/118 통과**.
- `node scripts/verify-browser.mjs`: **89/89 통과**(기존82+지속저장7).
- `node scripts/verify-new-ui.mjs`: **11/11 통과**.
- `node scripts/verify-navigation.mjs`: **14/14 통과**. 실제 터치·취소, 1440/768/390px 하단 배치, 현재 맵과 점, 카메라 역행 없음, 기둥 편집, 은하수 왕복, 게임 5종 완료.
- `node scripts/verify-department-work.mjs`: **6/6 통과**. 세 브라우저에서 저장/제출/교사 지급/초기화/0 포함 전원 확인/결과 표시와 휴대폰 닫기.
- 부서 지속 저장 검사에서 첫 확인 뒤 서버 재시작→남은 확인→단회 지급, 권한 거부·중복 거부·구성원 변경 취소·주간 합계도 확인했다. 순수 검사에 정확한 5일 경계·동시 초안·상한 실패 원자성을 포함했다.
- 실제 학급과 격리된 임시 서버/계정으로 검사했다. `.local`의 지도·모바일 부서 결과 스크린샷을 Astra가 직접 확인했다.
- 실제 재시작 후 새 공개 주소의 HTTPS·WebSocket·폴링·야간 입장 제한·Edge 로그인 화면 **7항목 통과**. 교사 키나 실제 학생 비밀번호를 공개 경로로 보내지 않았다.
- 원인 수정: 조이스틱 hidden/포인터 초기화, 숫자 게임 currentTarget, 반응 게임 상태·타이머, 늦은 dialog close 이벤트가 새 방향 입력을 지우는 문제. 이동 속도 증가로 옛 테스트의 고정 시간/상점 회피 경로가 맞지 않아 속도와 실제 위치 기준으로 수정했다.
- 새 통합 검사 초안은 읽기 요청에서 발생하지 않는 `room:state`를 기다려 멈췄다. 직접 스냅샷 검사로 바로잡고 정리 훅을 앞에 등록했다. 롤백 후 낡은 서버 객체를 쓰던 fixture도 갱신했다. 제품 오류와 검사 코드 오류를 구분했다.

## 5. 최종 점검(검수)

Astra 직접 검수 완료. 서버 역할·소속·근접·버전·금액을 검사하며 버튼 숨김에 권한을 의존하지 않는다. 지급 실패 시 일부만 지급하지 않고 재시작 후 같은 제안을 중복 지급하지 않는다. 공유 화면에 실적/분배를 다른 부서로 공개하지 않는다. 실적 지급 후 실제 빈 초안·미제출 상태로 돌아가는 것을 확인했다.

## 6. 미완료 사항과 다음 단계

- 요청 046~053 구현·자동 검증 완료, 사용자가 새로고침 후 직접 화면 확인하면 된다. 다음 요청 번호 **054**.
- 부원 전원에는 오프라인 부원과 0개 수령자도 포함한다. 가입/탈퇴 시 제안 취소 후 다시 정한다. 남은 잔액은 부서에 보관한다.
- 게임은 혼자 하는 연습용이다. 1:1 게임, 경험치/레벨 진화/별자리 원화·펫·퀘스트, 남은 교사 관리 기능은 후속 단계다.
- 실적을 교사가 보상하면 초안은 사라지며 보상 전 실적 이력 별도 보관은 구현하지 않았다(사용자 요청에 따라 백지 초기화). 분배 결과만 5일 보인다.
- 실제 서버를 기존 PID17028/터널20224 확인 후 재시작했다. 정지된 저장 잠금만 해제했다. 새 서버 PID18744는 숨김 실행, 저장 교실 유지. 새 무료 주소는 메인.md에 기록한다. 교사 키는 공개 연결 검사에 사용하지 않는다.
- 새 공개 주소: https://mortgage-emacs-authorities-retailers.trycloudflare.com . 기존 nerve-slot 주소는 종료했다. 로컬 교사는 `http://127.0.0.1:3000/`에서 기존 교실을 열면 된다.

## 7. Git 동기화 결과

작업 시작 시 `pull --ff-only origin main` 성공, 로컬 변경 없는 `72d43a8`에서 진행했다. 구현·검증·문서 32개 파일을 `f3536af`로 커밋하고 `origin/main`에 push 완료했다. HEAD=origin/main=실제 원격 main `f3536afced8e4ff008cbe8973b53a560b7f38b79`, 작업 트리 깨끗함을 확인했다. 이 동기화 확인 문서 변경도 후속 커밋으로 push한다. 강제 push는 사용하지 않았다.
