# 프로젝트 작업 시작 안내

- 이 폴더에서 작업하거나 이 프로젝트에 대한 사용자 요청을 처리하기 전에 같은 폴더의 `메인.md`를 먼저 읽는다.
- 새 요청을 `메인.md`의 요청 이력에 추가하고, 작업 후 진행 상태와 결과를 갱신한다.
- 상세 작업 기록은 `기록/YYYY-MM-DD_HH-mm-ss_작업핵심내용.md`로 저장한다. 한국 시간을 사용한다.
- 최신 사용자 지시를 우선하며, 구체적인 프로젝트 규칙과 미완료 사항은 `메인.md`를 참고한다.
- 사용자가 승인한 위임 규칙에 따라 간단한 작업은 새 하위 에이전트(gpt-5.6-luna, low)에 맡긴다. 필요한 문맥만 전달하고 중복 수행을 피한다. 세부 기준은 메인.md의 모델 선택과 하위 에이전트 위임 절을 따른다.
- 모든 작업의 최종 검수는 gpt-6-astra(Astra)가 맡는다. 메인이 Astra이면 직접 검수하고, 아니면 Astra 검수 에이전트에 필요한 결과와 근거만 전달한다. 검수 모델과 결과를 기록한다.
- Git 저장소는 https://github.com/rkdtjdwns1312-wq/space_metaverse.git (origin/main)이다. 작업 전에 메인.md와 Git 상태를 확인하고 pull --ff-only 후 최신 메인.md를 다시 읽는다. 작업 후 기록과 Astra 검수를 거쳐 commit/push하고 원격 동기화를 확인한다. 로컬 변경을 보존하며 강제 push는 하지 않는다. 실패 시 원인을 보고한다.
- **기본 인계 문서: `기록/2026-09-13_09-42-00_Codex_인수인계_지시서.md`** — 메인.md 다음으로 읽는다. 지속 저장 구현 이후의 변경은 `기록/2026-09-13_10-13-29_교실_지속저장_요청027.md`와 `기록/2026-09-15_00-52-00_지속저장_최종검증_재개.md`를 함께 읽는다. 이전 인계의 '요청 027 구현 예정'은 완료 기록으로 대체한다. 코드 변경 후 `node --test tests/*.test.js`(121개)와 `node scripts/verify-browser.mjs`(기존 82+저장 7건)를 통과시킨다.
- 이 저장소는 Claude Code 세션과 번갈아 작업한다. 두 도구 공통 규칙은 메인.md의 '협업 규칙' 절이 기준이다. Claude Code 전용 규칙은 `CLAUDE.md`에 있으며 Codex는 그 파일의 모델 규칙을 따르지 않는다(공통 규칙은 메인.md에만 적는다).
- 작업을 마칠 때 반드시 commit과 push까지 끝내 작업 트리를 깨끗하게 남긴다. 다음 작업이 Claude Code로 이어질 수 있다. 끝내지 못한 것은 메인.md '현재 상태와 미완료 사항'에 적는다.
- 기록 파일은 메인.md 협업 규칙의 7절 구성을 따르고, 상단에 '작업 도구: Codex'와 사용 모델·검수 모델을 적는다.
- Node.js 24.19.0 LTS가 `C:\Program Files\nodejs`에 정식 설치되어 있다(2026-09-12). 코드 변경 후 `node --test tests/*.test.js`와 `node scripts/verify-browser.mjs`를 통과시킨다. `교실-시작.cmd`는 CRLF 개행을 유지한다. `.env`와 `.local/`은 커밋하지 않는다.

- 최신 기능 인계: `기록/2026-09-15_01-59-00_전체화면_계정_친구_우주맵_확장.md`를 읽는다. 요청 036~043 구현 이후 `node scripts/verify-new-ui.mjs`(11개)도 실행한다. 요청 044에서 사용자 명시 승인 후 공개 실행 완료. 최신 운영 상태는 `기록/2026-09-15_02-08-00_Cloudflare_외부접속_개방_검증.md`와 메인.md를 읽는다. 이전 검토 거절 중 교사 키를 공개 경로로 전송하는 인증 검사는 제외했다.
- 요청 046~053 이후 최우선 추가 인계: `기록/2026-09-15_03-13-00_우주지도_신전_미니게임_부서실적.md`. 지도·터치·기둥·미니게임은 `node scripts/verify-navigation.mjs`(14개), 부서실적은 `node scripts/verify-department-work.mjs`(6개)도 검증한다. 운영 주소는 서버 재시작으로 바뀌므로 메인.md의 현재 주소를 확인한다.

- 실행기 복구 최신 인계: `기록/2026-09-15_08-03-00_서버실행_잠금복구_수정.md`. 종료 확인된 잠금만 자동 복구하며 실행 중/불명확 잠금은 보존한다.
