# 텍스트 파일은 저장소에 LF로 정규화하고, 체크아웃 시 OS 기본 개행을 따릅니다.
* text=auto
# Windows 배치 파일은 CRLF 개행이어야 chcp 65001 + goto 조합이 깨지지 않습니다.
*.cmd text eol=crlf
*.bat text eol=crlf
*.png binary
*.jpg binary
*.ico binary
